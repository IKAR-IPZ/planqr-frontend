services:
  frontend:
    build:
      context: .
    env_file:
      - ../.env
    ports:
      - "${FRONTEND_PORT:-443}:443"
    volumes:
      - ../certs:/etc/nginx/certs:ro
    restart: unless-stopped
